# Table

Ontology: **Interfaces**

Thin-ruled table primitives (`Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `TableCaption`) with wide-tracked uppercase headers. `DataGrid` builds on these.

```bash
pnpm dlx shadcn@latest add https://elements.micropreneur.dev/r/table.json
```

Install from the public Elements Free registry; the CLI writes source into your project.
